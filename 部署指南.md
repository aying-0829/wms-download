# WMS 仓储管理系统 · 本地部署指南

本系统为**私有化部署**，后端运行在你自己的电脑/服务器，数据不出局域网。

## 方式一：下载安装包（推荐，零配置）

1. 下载 `WMS-Setup.exe`（服务端版）或 `WMS-Client-Setup.exe`（桌面客户端）
2. 双击安装，自动注册 Windows 服务
3. 浏览器打开 `http://localhost:8000` 或桌面客户端
4. 演示账号：`admin / Admin@123456`

## 方式二：源码启动（开发者）

### 1. 启动后端
双击 `启动后端.bat`，自动：
- 安装 Python 依赖
- 初始化数据库 + 导入演示数据
- 启动服务 `http://localhost:8000`

### 2. 浏览器访问
打开 `app.html`（本仓库根目录），默认连 `http://localhost:8000/api/v1`
- 如需连接其他地址（局域网/服务器），在页面顶部修改并点「连接」

## 演示账号

| 账号 | 密码 | 权限 |
|------|------|------|
| admin | Admin@123456 | 系统管理员 |
| viewer | Viewer@123456 | 只读 |
| receiver | Receiver@123456 | 收货 |

## 常见问题

**Q: 打开官网看到的是产品介绍，不是系统？**
A: 官网是下载站 + 产品展示。系统需要下载安装包后在本地运行。

**Q: app.html 打开后白屏？**
A: 检查后端是否已启动（localhost:8000 能访问），或页面顶部填写正确的后端地址。

**Q: 后端启动报错？**
A: 确认 Python 3.11+ 已安装并加入 PATH，依赖已安装（pip install -r requirements.txt）。
